# OverView
DynamoDB Migrator.

## Migration Filename Format

```shell
{version}_{title}.{command}.json
{version}_{title}.{command}.json
```

### command

DynamoDB commands.

- create_table
- delete_table